/**
 * Bundled silk changelog generator — the action-shipped module the changesets
 * engine `import()`s when Phase 1 versions natively with no consumer
 * `node_modules`. Mapped from the ids `@savvy-web/changelog`,
 * `@savvy-web/silk/changesets/changelog`, and
 * `@savvy-web/changesets/changelog` (see `src/utils/native-version.ts`).
 */

import { Changesets } from "@savvy-web/silk-effects";

export default Changesets.changelogFunctions;
